# 链表类实现

本项目实现了一个自定义的双向链表模板类 `List`，该类提供了常见的链表操作，例如在链表头部和尾部添加或删除元素、遍历链表、检查链表大小和是否为空等。


## 新增功能

1. **`resize` 功能**
   - `resize` 函数允许用户动态更改链表的大小。如果新的大小大于当前链表大小，链表会根据提供的默认值进行扩展。如果新的大小小于当前大小，则从链表尾部开始移除多余的元素。
   
2. **`find` 功能**
   - `find` 函数允许用户查找val并返回val所在的第一个位置，如果不在列表中，返回-1/
   


